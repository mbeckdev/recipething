import React from 'react';

interface Props {
    name: string;
}

const Tools = () => {
    return (
        <div>
tools
        </div>
    )
}
// const Intro: React.FC<Props> = ({ name }) => {

export default Tools;
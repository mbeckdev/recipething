import React from 'react';

interface Props {
    name: string;
}

const Ingredients = () => {
    return (
        <div>
            Ingredients
        </div>
    )
}
// const Intro: React.FC<Props> = ({ name }) => {

export default Ingredients;
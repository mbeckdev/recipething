import React from 'react';

interface Props {
    name: string;
}

const Intro = () => {
// const Intro = () => {
    return (
        <div>
            intro
        </div>
    )
}
// const Intro: React.FC<Props> = ({ name }) => {

export default Intro;